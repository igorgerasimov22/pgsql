# PostgreSQL configuration file
listen_addresses = '*'
port = 5432
wal_level = replica
max_wal_senders = 3
wal_keep_segments = 64
hot_standby = on
archive_mode = on
archive_command = 'cp %p /var/lib/postgresql/archive/%f'